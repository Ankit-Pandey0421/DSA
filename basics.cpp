#include<iostream>
using namespace std;
int main()
{
    /*     

    int a = 123;
    cout << a << endl;

    char b = 'v';
    cout<< b << endl;

    bool bl = true;
    cout<< bl << endl;

    float f = 1.2 ;
    cout<< f << endl ;

    double d = 1.23;
    cout<< d << endl;

    int size = sizeof(d);
    cout<< "Size of a is : " << size <<endl;
    */

      //TYPECASTING 

    /*                      IMPLICIT                        
    int a = 'a';
    cout<< a << endl;

    char ch = 90;
    cout << ch << endl;

    char ch1 = 123456; 
    char << ch << endl; 
    // 123456 is an integer last byte of of it gives 64
    // hence only 64 is stored in the char type variable named ch1
    // this is IMPLICIT typecasting
     
    */
   
    // UNSIGNED
    /*
    int a = - 112  ;
    unsigned b = - 112;
    cout<< a << endl;
    cout << b << endl; 
    */

    // OPERATORS 

    /*
    int a = 2.0  ;
    cout<< a << endl;                       // 0
    cout << 2.0/5 << endl;                  // 0.4
    */

    // RELATIONAL OPERATORS
   /*
   int a = 2;
   int b = 3;

   bool first = (a==b);
   cout << first << endl;

   bool second = (a>b);
   cout << second << endl;

   bool third = (a<b);
   cout << third << endl;

   bool fourth = (a>=b);
   cout << fourth << endl;

   bool fifth = (a<=b);
   cout << fifth << endl;

   bool sixth = (a!=b);
   cout << sixth << endl;
   */
  
}
